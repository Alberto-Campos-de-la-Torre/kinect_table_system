# ===================================
# KINECT TABLE SYSTEM - REQUIREMENTS
# ===================================
# Compatible con Python 3.8 - 3.11
# Sistema operativo: Ubuntu 20.04+

# Core Dependencies
numpy>=1.24.0,<2.0
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
Pillow>=10.0.0

# Kinect Interface (Ubuntu/Linux)
# Requiere instalacion de sistema:
#   sudo apt-get install libfreenect-dev freenect libusb-1.0-0-dev python3-freenect
#
# El modulo Python se instala con:
#   pip install freenect
# O usar el wrapper de ctypes incluido en modules/kinect_capture.py

# Deep Learning & Object Detection
torch>=2.1.0
torchvision>=0.16.0
ultralytics>=8.0.196  # YOLOv8

# Para GPU NVIDIA con CUDA (opcional):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Gesture Recognition
mediapipe>=0.10.7

# 3D Processing
open3d>=0.17.0

# Scientific Computing
scipy>=1.11.0
scikit-learn>=1.3.0
scikit-image>=0.22.0

# Visualization & GUI
pygame>=2.5.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
colorama>=0.4.6

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Logging & Monitoring
loguru>=0.7.2

# Data Storage
pandas>=2.1.0

# Image Processing Extras
imageio>=2.31.0
pytesseract>=0.3.10  # Para OCR (opcional, requiere: sudo apt-get install tesseract-ocr)

# Networking
requests>=2.31.0
websockets>=12.0
aiohttp>=3.9.0

# Development Tools
black>=23.11.0
flake8>=6.1.0
pylint>=3.0.0
ipython>=8.17.0
jupyter>=1.0.0

# Performance
numba>=0.58.0  # JIT compilation

# ===================================
# INSTALACION EN UBUNTU
# ===================================
#
# 1. DEPENDENCIAS DEL SISTEMA:
# ----------------------------
# sudo apt-get update
# sudo apt-get install -y \
#     libfreenect-dev \
#     freenect \
#     libusb-1.0-0-dev \
#     python3-freenect \
#     python3-dev \
#     python3-pip \
#     python3-venv \
#     build-essential \
#     cmake \
#     tesseract-ocr
#
# 2. PERMISOS PARA KINECT:
# ------------------------
# sudo usermod -a -G plugdev $USER
# sudo usermod -a -G video $USER
#
# Crear reglas udev:
# sudo bash -c 'cat > /etc/udev/rules.d/51-kinect.rules << EOF
# SUBSYSTEM=="usb", ATTR{idVendor}=="045e", ATTR{idProduct}=="02b0", MODE="0666"
# SUBSYSTEM=="usb", ATTR{idVendor}=="045e", ATTR{idProduct}=="02ae", MODE="0666"
# SUBSYSTEM=="usb", ATTR{idVendor}=="045e", ATTR{idProduct}=="02ad", MODE="0666"
# EOF'
# sudo udevadm control --reload-rules && sudo udevadm trigger
#
# 3. CUDA (opcional, para GPU NVIDIA):
# ------------------------------------
# Instalar CUDA Toolkit desde:
# https://developer.nvidia.com/cuda-downloads
#
# Luego instalar PyTorch con CUDA:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# ===================================
